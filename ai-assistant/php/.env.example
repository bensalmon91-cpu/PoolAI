# PoolAIssistant AI assistant PHP config
# Copy this file to .env and fill in real values. Never commit .env.

# Database Configuration
DB_HOST=localhost
DB_NAME=u931726538_PoolAIssistant
DB_USER=u931726538_mbs_modproject
DB_PASS=

# Session Configuration
SESSION_NAME=mod_admin_session
SESSION_LIFETIME=3600

# Upload limits (bytes) - 50MB
MAX_UPLOAD_SIZE=52428800

# Rate Limiting
LOGIN_MAX_ATTEMPTS=5
LOGIN_LOCKOUT_TIME=900

# Device Provisioning Secret (generate: python -c "import secrets; print(secrets.token_hex(32))")
BOOTSTRAP_SECRET=

# JWT secret (generate: python -c "import secrets; print(secrets.token_hex(32))")
JWT_SECRET=
